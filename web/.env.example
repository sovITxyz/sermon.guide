# Base URL of the FastAPI backend. SERVER-SIDE ONLY — read exclusively by Next
# route handlers and server components (see lib/config.ts). It is never exposed
# to the browser: there is no NEXT_PUBLIC_ prefix, so the bundler will not inline
# it into client JS, and the JWT it fronts lives only in the HttpOnly cookie.
API_BASE_URL=http://localhost:8000
